##### Note:
Users with only cuda:0 may encounter errors when running the pre-trained models for testing. To fix this, the downloaded models can be converted to cuda:0 using change_gpu.py located in the /save_model directory.
